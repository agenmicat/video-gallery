class VideoGallery{constructor(){this.videos=[{id:1,title:"Demo MP4",thumbnail:"https://img.youtube.com/vi/dQw4w9WgXcQ/maxresdefault.jpg",url:"https://sample-videos.com/zip/10/mp4/SampleVideo_1280x720_1mb.mp4",type:"mp4",duration:"1:30"},{id:2,title:"Demo HLS",thumbnail:"https://via.placeholder.com/1280x720/FF0000/FFFFFF?text=HLS",url:"https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8",type:"hls",duration:"2:15"},{id:3,title:"Big Buck Bunny",thumbnail:"https://img.youtube.com/vi/9bZkp7q19f0/maxresdefault.jpg",url:"https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4",type:"mp4",duration:"0:45"}];this.init();}init(){this.renderVideos();this.setupEventListeners();}renderVideos(videos=this.videos){const grid=document.getElementById('videoGrid');grid.innerHTML='';videos.forEach(video=>{const card=this.createVideoCard(video);grid.appendChild(card);});}createVideoCard(video){const card=document.createElement('div');card.className='video-card';card.dataset.videoId=video.id;card.innerHTML=`<div class="video-thumbnail"><img src="${video.thumbnail}" alt="${video.title}" loading="lazy"></div><div class="video-info"><h3>${video.title}</h3><p>${video.duration}</p></div>`;return card;}setupEventListeners(){document.getElementById('searchInput').addEventListener('input',e=>this.searchVideos(e.target.value));document.getElementById('videoGrid').addEventListener('click',e=>{if(e.target.closest('.video-card')){const id=e.target.closest('.video-card').dataset.videoId;this.openModal(id);}});document.querySelector('.close').addEventListener('click',()=>this.closeModal());window.addEventListener('click',e=>{if(e.target.classList.contains('modal'))this.closeModal();});document.addEventListener('keydown',e=>{if(e.key==='Escape')this.closeModal();});}searchVideos(query){const filtered=this.videos.filter(v=>v.title.toLowerCase().includes(query.toLowerCase()));this.renderVideos(filtered);}openModal(id){const video=this.videos.find(v=>v.id==id);if(!video)return;document.getElementById('videoTitle').textContent=video.title;const player=document.getElementById('videoPlayer');player.innerHTML='';video.type==='hls'?this.loadHLS(video.url):this.loadMP4(video.url);document.getElementById('videoModal').style.display='block';document.body.style.overflow='hidden';}loadMP4(url){document.getElementById('videoPlayer').innerHTML=`<video controls autoplay muted><source src="${url}" type="video/mp4">Browser tidak support video.</video>`;}loadHLS(url){const player=document.getElementById('videoPlayer');player.innerHTML=`<video id="hlsPlayer" controls autoplay muted></video>`;if(Hls.isSupported()){const hls=new Hls();hls.loadSource(url);hls.attachMedia(document.getElementById('hlsPlayer'));}else if(document.getElementById('hlsPlayer').canPlayType('application/vnd.apple.mpegurl')){document.getElementById('hlsPlayer').src=url;}}closeModal(){document.getElementById('videoModal').style.display='none';document.body.style.overflow='auto';document.getElementById('videoPlayer').innerHTML='';}}const script=document.createElement('script');script.src='https://cdn.jsdelivr.net/npm/hls.js@latest';script.onload=()=>new VideoGallery();document.head.appendChild(script);